---
name: typescript-skill
description: TypeScript/Node: types, tsconfig, ESM/CJS, Next.js, strict typing, refactors.
---

# TypeScript Skill
- Prefer `strict` tsconfig; avoid `any` unless justified.
- Match existing project module system (ESM vs CJS).
- Use existing lint/test commands from package.json.
- For refactors: find references via code graph first, then edit.
