---
name: ci-skill
description: CI/CD: GitHub Actions, pipelines, caching, matrix builds, release workflows.
---

# CI Skill
- Prefer reusable workflows; cache deps; fail fast.
- Pin action versions (sha or full tag).
- Secrets via CI secret store only.
- Keep PR checks minimal and release pipelines separate.
