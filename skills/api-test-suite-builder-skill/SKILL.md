---
name: api-test-suite-builder-skill
description: Design and build API test suites (REST/GraphQL), contract tests, smoke checks.
---

# API Test Suite Builder
- Map endpoints first (OpenAPI/routes graph).
- Cover auth, validation, happy path, and critical error codes.
- Keep tests deterministic; isolate fixtures.
