---
name: github-actions-skill
description: Author and fix GitHub Actions workflows, actions, and marketplace actions usage.
---

# GitHub Actions Skill
- Workflows under `.github/workflows/`.
- Use `concurrency` to cancel outdated PR runs.
- Least-privilege `permissions:`.
- Test with `act` only if available; otherwise validate YAML + expressions carefully.
